<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Vista Gráfica Principal -->
        <record id="view_game_statistics_graph" model="ir.ui.view">
            <field name="name">game.statistics.graph</field>
            <field name="model">game.player</field>
            <field name="arch" type="xml">
                <graph string="🎮 Estadísticas del Juego" type="bar" stacked="True" sample="1">
                    <field name="name"/>
                    <field name="total_wins" type="measure"/>
                    <field name="total_matches" type="measure"/>
                    <field name="coin_balance" type="measure"/>
                </graph>
            </field>
        </record>

        <!-- Vista Pivot para análisis -->
        <record id="view_game_statistics_pivot" model="ir.ui.view">
            <field name="name">game.statistics.pivot</field>
            <field name="model">game.player</field>
            <field name="arch" type="xml">
                <pivot string="📊 Análisis Detallado" sample="1">
                    <field name="name" type="row"/>
                    <field name="level" type="measure"/>
                    <field name="total_wins" type="measure"/>
                    <field name="total_matches" type="measure"/>
                    <field name="coin_balance" type="measure"/>
                </pivot>
            </field>
        </record>

        <!-- Acción para las Estadísticas -->
        <record id="action_game_statistics" model="ir.actions.act_window">
            <field name="name">📊 Estadísticas del Juego</field>
            <field name="res_model">game.player</field>
            <field name="view_mode">graph,pivot</field>
            <field name="view_id" ref="view_game_statistics_graph"/>
        </record>

        <!-- Menú para Estadísticas -->
        <menuitem 
            id="menu_game_statistics"
            name="📊 Estadísticas"
            parent="menu_game_api_root"
            action="action_game_statistics"
            sequence="2"/>
    </data>
</odoo>